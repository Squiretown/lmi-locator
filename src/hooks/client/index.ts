
export { useSavedAddresses } from '../useSavedAddresses';
export { useClientActivity } from '../useClientActivity';
export { usePropertySearch } from '../usePropertySearch';
export { usePropertyWorkflow } from '../usePropertyWorkflow';

// Create a file for convenient imports of client-related hooks
